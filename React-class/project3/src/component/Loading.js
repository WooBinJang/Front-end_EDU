import React from "react";

function Loading() {
  return (
    <div
      className="Loading"
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",

        height: "100vh",
      }}
    >
      <h1>Loading....</h1>
    </div>
  );
}

export default Loading;
